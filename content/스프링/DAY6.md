

백엔드 

일관성 : 데이터 1을 넣었을 때 동일한 데이터 결과값이 나오는 것/ F(x) -> F(F(x))
무결성 : 사전에 계획한 대로 결과가 나와야하는 것. 가령 abc를 제목으로 넣었는데 adc가 저장되는 것은 무결성을 위배한 것이다.
영속성 : 소프트웨어의 시간 흐름에 데이터는 계속 누적이 될 것이지만 중단되고 다시 실행되어도 해당하는 데이터가 유지되는 것. 즉, 프로그램이 종료되더라도 다시 시작할 수 있는 능력. 보통 데이터 베이스를 통해 해결 (SQL, DBMS. 비정형, 실시간 데이터)

비즈니스 모델 - DATA 그 사이에서 DAO를 통해 연결
복잡한 데이터를 추상화.


  

// 웹프로그래밍에서 서비스는 순서대로 실행되지 않음, 공유데이터 사용

// 동기화 : 여러개의 작업 순서를 결정하는 것 항상 순서대로 작동되지 않기 때문. 데이터를 맞춰주는 것은 절대 아니다.

서비스 호출 순서는 동기화가 될 수 없다.


가비지 컬렉션 대상이 되는 시기는?

jsp 소스 코드가 끝난 시점. 객체 생성후 사용을 안하기 때문 (e.g. add_emp.jsp) 모델이 가지고 있는 사원 모델이 삭제된다. 이것은 영속성이 없다는 것. -> 파일 시스템, DBMS 사용



EMPList를 그대로 return하게도면? -> empList는 사원정보를 담아 놓는데 어딘가에서 사용하다가 사용을 못하게 삭제될 수 있다. 즉, private인데도 정보은닉이 되지  않는 문제 -> new ArrayList<>  (empList);를 새로 만들어서 리턴

일반화와 추상화를 잘 써서 리턴해야한다.

EmpList를 Default로 했는데 그 이유는?

디폴트 접근제한자 
자바언어는 소프트웨어 재사용을 통해 비용을 줄일려고 하나는데 public이 많으면 이를 어기는 것,. 그렇기 때문에 method들이 Default 접근 제한자 사용
Default 접근 제한자의 범위는 같은 Directory 기능 수정시 같은 디렉토리만 보면 된다.

데이터 조회 전체 후 필요한 것만 필터링하는 것이 추후 필터링에 좋을 수도?

```
dao.list(); -> filter
```

일반화하고 추상화하는 부분

